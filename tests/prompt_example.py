"""
Пример промпта/задачи в формате, который ожидает Space (поле `text_input`).
Экспортирует функцию get_sample_problem(), которую использует тест.
"""

def get_sample_problem():
    # Простая тестовая задача с формулой LaTeX (KaTeX-compatible)
    return (
        "Дано тело массой $m=2$ кг, движется с постоянным ускорением $a=3\,\mathrm{м/с^2}$. "
        "Найдите силу, действующую на тело, и приведите расчёт с формулами (KaTeX)."
    )


if __name__ == '__main__':
    print(get_sample_problem())
