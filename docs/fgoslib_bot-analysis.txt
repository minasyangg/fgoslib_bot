<?xml version='1.0' encoding='utf-8'?>
<source type="github_repository"><commit>unknown</commit><file name="Dockerfile.hf">python:3.12-slim label maintainer="fgoslib" workdir /app # install ca-certificates tls downloads run apt-get update \ &amp;&amp; apt-get install --no-install-recommends ca-certificates \ &amp;&amp; rm -rf /var/lib/apt/lists/* copy requirements.hf.txt /app/requirements.hf.txt run pip install --no-cache-dir -r /app/requirements.hf.txt copy . /app env pythonunbuffered=1 env port=8080 expose 8080 cmd ["python", "hf_worker.py"]</file><file name="Dockerfile.render">python:3.12-slim # create app dir workdir /usr/src/app # install system deps needed playwright run apt-get update &amp;&amp; apt-get install --no-install-recommends \ ca-certificates \ wget \ gnupg \ curl \ libnss3 \ libatk1.0-0 \ libatk-bridge2.0-0 \ libcups2 \ libxss1 \ libasound2 \ libgbm1 \ libpangocairo-1.0-0 \ libx11-6 \ libxcomposite1 \ libxrandr2 \ libxdamage1 \ libxfixes3 \ libxkbcommon0 \ libxshmfence1 \ libfontconfig1 \ fonts-liberation \ &amp;&amp; rm -rf /var/lib/apt/lists/* # copy requirements install copy requirements.render.txt ./ run pip install --no-cache-dir -r requirements.render.txt # install playwright browsers deps run playwright install --with-deps || playwright install chromium || true # copy app copy render_service.py ./ # download katex assets build container serve locally run mkdir -p ./static/katex &amp;&amp; \ curl -l https://registry.npmjs.org/katex/-/katex-0.16.8.tgz katex.tgz &amp;&amp; \ tar -xzf katex.tgz package/dist &amp;&amp; \ mv package/dist/* ./static/katex/ || true &amp;&amp; \ rm -rf katex.tgz package || true env pythonunbuffered=1 # render.com expects web services bind port. default 8080. env port=8080 expose 8080 cmd ["python", "render_service.py"]</file><file name="README_bot.md"># fgoslib telegram bot ## telegram- , gradio- huggingface flask. redis, , (markdown pdf). --- ## - `my_bot.py` telegram-. - `monitor_backend.py` flask- . - `monitor.html` - . - `.env` (, url). - `requirements.txt` python. --- ## ### 1. ```sh git clone &lt;repo-url&gt; cd fgoslib_bot python venv venv source venv/bin/activate # venv\scripts\activate windows pip install -r requirements.txt ``` ### 2. `.env` ( ): ``` telegram_token=&lt;__&gt; hf_token=&lt;_huggingface_token&gt; upstash_redis_url=&lt;_upstash_redis_url&gt; ``` ### 3. telegram- ```sh python my_bot.py ``` ### 4. ```sh python monitor_backend.py ``` : [http://localhost:8080](http://localhost:8080) --- ## gradio- huggingface gradio api, huggingface spaces. api [`call_hf_api`](my_bot.py): - url api `hf_api_url` ( : `https://hf.space/embed/mingg93/fgoslib-qwen3/api/predict/`). - huggingface (`hf_token`). - : ```json { "task_text": " ", "user_prompt": ". ", "images": ["file_id1", "file_id2"], "output_format": "md" // "pdf" } ``` - api `text` ( `pdf` pdf). --- ## hugo cms hugo: 1. ** :** - `monitor.html` hugo- (, iframe): ```html &lt;iframe src="https://&lt;render-monitor-service-url&gt;" width="100%" height="600"&gt;&lt;/iframe&gt; ``` - js `monitor.html` . 2. ** :** - telegram-: ```html href="https://t.me/&lt;_&gt;"&gt; ``` - gradio api proxy fetch ( cors ): ```js fetch('https://hf.space/embed/mingg93/fgoslib-qwen3/api/predict/', { ... }) ``` --- ## - `telegram_token` telegram- - `hf_token` huggingface api token - `upstash_redis_url` redis url (upstash) --- ## render - **web service**: (`monitor_backend.py`) - **background worker**: (`my_bot.py`) - `upstash_redis_url` --- ## mit</file><file name="hf_worker.py">#!/usr/bin/env python3 """hf worker: consumes `hf_queue` forwards tasks huggingface space (gradio api). behavior: - brpop redis `hf_queue` json tasks - validate images (&lt;= max_images) user_prompt length - basic moderation user_prompt using small blacklist - post payload `hf_api_url` bearer `hf_token` - handle response containing `pdf_url` `pdf_base64` download &amp; forward) - store result redis `task_pdf_result:&lt;task_id&gt;` ttl - send pdf telegram chat via bot api ready configuration via environment variables: - upstash_redis_url (required) - hf_token (required) - hf_api_url (required) e.g. https://hf.space/embed/&lt;owner&gt;/&lt;repo&gt;/api/predict endpoint - telegram_token (required sending file) - max_images (default 5) - max_prompt_len (default 1000) - redis_ttl (default 900) - worker_concurrency (default 3) - hf_timeout (default 180) """ import os import json import time import logging import base64 import requests import threading import asyncio import hashlib concurrent.futures import threadpoolexecutor import redis try: # aiohttp used health endpoint aiohttp import web except exception: web = none try: gradio_client import client, handle_file except exception: client = none handle_file = none try: # apperror class exists gradio_client package gradio_client.client import apperror gradioapperror except exception: gradioapperror = none class hfquotaerror(exception): """raised hf space reports quota / resource limit error.""" def __init__(self, msg: str, retry_after_seconds: int | none = none): super().__init__(msg) self.retry_after_seconds = retry_after_seconds logging.basicconfig(level=logging.info) logger = logging.getlogger('hf_worker') # worker start time uptime metric start_time = time.time() upstash_redis_url = os.environ.get('upstash_redis_url') upstash_redis_url: raise runtimeerror('upstash_redis_url required') hf_token = os.environ.get('hf_token') hf_api_url = os.environ.get('hf_api_url') telegram_token = os.environ.get('telegram_token') hf_token: logger.warning('hf_token set; gradio_client calls fail') else: # log masked token prefix verify loaded correctly token_preview = hf_token[:8] + '...' len(hf_token) &gt; 8 else '***' logger.info('hf_token loaded: (len=%d)', token_preview, len(hf_token)) hf_api_url: logger.info('hf_api_url set; use gradio_client (recommended)') telegram_token: logger.warning('telegram_token set; cannot send telegram messages') r = redis.redis.from_url(upstash_redis_url, decode_responses=false) max_images = int(os.environ.get('max_images', '5')) max_prompt_len = int(os.environ.get('max_prompt_len', '1000')) redis_ttl = int(os.environ.get('redis_ttl', '900')) worker_concurrency = int(os.environ.get('worker_concurrency', '3')) hf_timeout = int(os.environ.get('hf_timeout', '180')) retries = int(os.environ.get('hf_retries', '2')) hf_max_retries = int(os.environ.get('hf_max_retries', '5')) # gradio/space settings hf_space = os.environ.get('hf_space', 'mingg93/fgoslib-qwen3') hf_api_name = os.environ.get('hf_api_name', '/solve_problem') # enforce using gradio_client provided hf_token http fallback allowed. use_gradio_client = true hf_allow_http_fallback = false # small blacklist extra prompts (simple approach) blacklist = [ 'bomb', 'terror', 'drugs', 'sex', 'assault', 'kill', 'murder', 'porn', 'hate', 'racist', 'illegal' ] tg_api_base = f'https://api.telegram.org/bot{telegram_token}/' telegram_token else none def schedule_delayed_task(task_obj: dict, delay_seconds: int): """schedule task later execution using redis sorted set. score = unix timestamp task becomes ready. """ try: ready_at = int(time.time() + max(0, int(delay_seconds))) r.zadd('hf_delayed', {json.dumps(task_obj): ready_at}) logger.info('scheduled task retry task_obj.get('task_id'), ready_at, delay_seconds) except exception: logger.exception('failed schedule delayed task') def move_due_delayed_tasks(): """background loop: move due tasks `hf_delayed` `hf_queue`. runs daemon thread. """ try: true: try: = int(time.time()) # get tasks score &lt;= items = r.zrangebyscore('hf_delayed', 0, items: v items: try: # remove delayed set push back queue removed = r.zrem('hf_delayed', v) removed: r.lpush('hf_queue', v) logger.info('moved delayed task back hf_queue') except exception: logger.exception('failed move delayed task') time.sleep(5) except exception: logger.exception('delayed-task mover error') time.sleep(5) except exception: logger.exception('delayed-task mover fatal error') def moderate_prompt(prompt: str) -&gt; bool: """return true prompt allowed, false blocked.""" prompt: return true low = prompt.lower() w blacklist: w low: return false return true def send_telegram_document(chat_id: int, file_bytes: bytes, filename: str = 'solution.pdf') -&gt; bool: tg_api_base: logger.warning('telegram token set; cannot send file') return false try: files = {'document': (filename, file_bytes, 'application/pdf')} data = {'chat_id': str(chat_id)} resp = requests.post(tg_api_base + 'senddocument', data=data, files=files, timeout=30) resp.status_code // 100 == 2: logger.info('sent pdf chat chat_id) return true else: logger.warning('telegram send failed: resp.status_code, resp.text) return false except exception: logger.exception('failed send telegram document') return false def call_hf_api(payload: dict) -&gt; dict: """post payload hf api url return response json.""" headers = {'authorization': f'bearer {hf_token}'} hf_token else {} urls_to_try = [] hf_api_url: # try hf_api_url directly looks like api endpoint '/api/' hf_api_url: urls_to_try.append(hf_api_url) else: logger.warning('configured hf_api_url look like api endpoint; skipping direct post avoid 405: hf_api_url) # try embed-style api path try: owner, repo = hf_space.split('/') embed_url = f"https://hf.space/embed/{owner}/{repo}/api/predict{hf_api_name}" urls_to_try.append(embed_url) direct_url = f"https://{owner}-{repo}.hf.space/api/predict{hf_api_name}" urls_to_try.append(direct_url) except exception: # malformed hf_space; skip pass last_exc = none url urls_to_try: try: logger.info('calling hf http api url) resp = requests.post(url, headers=headers, json=payload, timeout=hf_timeout) resp.raise_for_status() try: return resp.json() except exception: logger.exception('hf response json url) return {'error': 'invalid_response', 'text': resp.text} except requests.exceptions.httperror # 405, try next candidate url; otherwise record continue logger.warning('hf http error url, last_exc = continue except exception e: logger.exception('hf api call failed url) last_exc = e continue # attempts failed logger.error('all hf http api attempts failed') last_exc: raise last_exc raise runtimeerror('hf api call failed endpoint configured)') def download_url(url: str) -&gt; bytes: try: r = requests.get(url, timeout=60) r.raise_for_status() return r.content except exception: logger.exception('failed download url) raise def download_telegram_file_if_needed(file_id: str) -&gt; str: given telegram file_id, download return local path. otherwise, return none. assumes telegram_token set env. """ file_id isinstance(file_id, str): return none file_id.startswith('http') file_id.startswith('data:'): return none token = os.environ.get('telegram_token') token: logger.warning('telegram_token set; cannot download telegram file_id') return none try: info = requests.get(f'https://api.telegram.org/bot{token}/getfile?file_id={file_id}', timeout=15) info.raise_for_status() j = info.json() file_path = j['result']['file_path'] url = f'https://api.telegram.org/file/bot{token}/{file_path}' resp = requests.get(url, timeout=60) resp.raise_for_status() import tempfile, os suffix = os.path.splitext(file_path)[1] '' tmp = tempfile.namedtemporaryfile(delete=false, suffix=suffix) tmp.write(resp.content) tmp.close() return tmp.name except exception: logger.exception('failed download telegram file_id file_id) return none def call_hf_via_gradio_client(task_text: str, images: list, user_prompt: str): """call hf space via gradio_client.client return dict results. expected return keys: 'markdown', 'file_bytes' none), 'time' """ client none: raise runtimeerror('gradio_client installed') # hf_token provided env, pass gradio_client.client # requests zerogpu spaces made authenticated token # consume account's quota/priority rather unauthenticated quota. # require hf_token requests made behalf configured hf account. hf_token: logger.error('hf_token required gradio_client calls') raise runtimeerror('hf_token required') token_preview = hf_token[:8] + '...' len(hf_token) &gt; 8 else '***' logger.info('creating gradio client space=%s token=%s', hf_space, token_preview) # prefer creating plain client(hf_space) let gradio_client read hf_token # environment. matches local snippet used testing: # # gradio_client import client, handle_file # client = client("mingg93/fgoslib-qwen3") # result = client.predict(..., api_name="/solve_problem") # # ensure hf_token exported client uses authenticated token. try: # ensure env vars present client() attempts resolve space try: hf_token: os.environ.setdefault('hf_token', hf_token) os.environ.setdefault('huggingface_hub_token', hf_token) logger.info('exported hf env vars client creation (prefix=%s)', token_preview) except exception: logger.exception('failed set hf env vars client creation') client = none # try passing token constructor kwarg several common names candidates = ['token', 'hf_token', 'api_token', 'auth', 'hf_api_token'] tried = [] hf_token: name candidates: try: kwargs = {name: hf_token} client = client(hf_space, **kwargs) logger.info('gradio client created constructor kwarg name) break except typeerror: tried.append(name) continue except exception: logger.exception('failed create gradio client kwarg name) client = none # final fallback: set multiple env vars try plain client() client none: # ensure env var present older clients read env os.environ['hf_token'] = hf_token os.environ['huggingface_hub_token'] = hf_token token_preview = hf_token[:8] + '...' len(hf_token) &gt; 8 else '***' logger.info('hf_token set env gradio_client: token_preview) try: client = client(hf_space) logger.info('gradio client created successfully space=%s using env var fallback', hf_space) except exception: logger.exception('failed create gradio client env var fallback') raise else: # token provided; attempt plain client() client = client(hf_space) logger.info('gradio client created successfully space=%s token)', hf_space) # diagnostic: check whoami token log short client repr try: try: = requests.get('https://huggingface.co/api/whoami-v2', headers={'authorization': f'bearer {hf_token}'}, timeout=8) try: j = who.json() logger.info('hf whoami: id=%s ispro=%s', j.get('id'), j.get('ispro')) except exception: logger.info('hf whoami raw: who.text[:400]) except exception: logger.exception('hf whoami check failed') except exception: # swallow diagnostics errors pass try: logger.info('gradio client repr: repr(client)[:300]) except exception: pass except exception: logger.exception('failed create gradio client') raise # prepare image_input: gradio image component expects dict 'path' 'url' image_input = none images: img = images[0] isinstance(img, dict): # already structured image_input = img elif isinstance(img, str): img.startswith('http'): image_input = {'url': img} elif img.startswith('data:'): # save data url temp file try: import base64, tempfile, header, b64 = img.split(',', 1) ext = '.png' = re.search(r'data:image/([a-za-z0-9]+);', header) ext = '.' + m.group(1) tmp = tempfile.namedtemporaryfile(delete=false, suffix=ext) tmp.write(base64.b64decode(b64)) tmp.close() image_input = {'path': tmp.name} except exception: logger.exception('failed write data url temp file') else: # assume telegram file_id local = download_telegram_file_if_needed(img) local: image_input = {'path': local} # call gradio client try: # debug: log send space (helps verify parity local test) try: logger.debug('gradio predict args: api_name=%s, text_len=%s, image_input_preview=%s', hf_api_name, len(task_text ''), repr(image_input)[:400]) except exception: # fail debug logging fails logger.debug('gradio predict args: (failed build preview)') res = client.predict( text_input=task_text, image_input=image_input, api_name=hf_api_name ) # res expected [markdown_str, filepath_or_obj, time_str] markdown = none file_bytes = none gen_time = none file_name = none try: markdown = res[0] file_part = res[1] gen_time = res[2] len(res) &gt; 2 else none # file_part dict 'url' local path isinstance(file_part, dict): url = file_part.get('url') file_part.get('path') url isinstance(url, str) url.startswith('http'): file_bytes = download_url(url) try: file_name = os.path.basename(url) except exception: file_name = none elif url isinstance(url, str) os.path.exists(url): open(url, 'rb') f: file_bytes = f.read() try: file_name = os.path.basename(url) except exception: file_name = none elif isinstance(file_part, str): file_part.startswith('http'): file_bytes = download_url(file_part) try: file_name = os.path.basename(file_part) except exception: file_name = none elif os.path.exists(file_part): open(file_part, 'rb') f: file_bytes = f.read() try: file_name = os.path.basename(file_part) except exception: file_name = none else: # maybe base64 inline content; filename file_name = none except exception: logger.exception('failed parse gradio client result') return {'markdown': markdown, 'file_bytes': file_bytes, 'time': gen_time, 'file_name': file_name} except exception e: # gradio apperror may able detect quota issues try: gradioapperror none isinstance(e, gradioapperror): msg = str(e) logger.warning('gradio apperror: msg) low = msg.lower() 'quota' low 'exceeded gpu quota' low 'gpu quota' low: # try parse retry time like 'try hh:mm:ss' import retry = none = re.search(r'try (\d{1,2}:\d{2}:\d{2})', msg) h, mi, = m.group(1).split(':') retry = int(h)*3600 + int(mi)*60 + int(s) raise hfquotaerror(msg, retry_after_seconds=retry) except hfquotaerror: # bubble quota error raise except exception: # quota apperror; fall generic handler pass logger.exception('gradio client call failed') raise def save_result_to_redis(task_id: str, result: dict): key = f'task_pdf_result:{task_id}' try: r.set(key, json.dumps(result), ex=redis_ttl) except exception: logger.exception('failed save result redis') def process_task(item: dict): task_id = item.get('task_id') chat_id = item.get('chat_id') task_text = item.get('task_text', '') images = item.get('images', []) [] user_prompt = item.get('user_prompt', '') '' logger.info('processing task chat task_id, chat_id) # validate len(images) &gt; max_images: images = images[:max_images] len(user_prompt) &gt; max_prompt_len: user_prompt = user_prompt[:max_prompt_len] allowed = moderate_prompt(user_prompt) allowed: # notify user drop prompt try: tg_api_base chat_id: msg = {'chat_id': str(chat_id), 'text': ' ; .'} requests.post(tg_api_base + 'sendmessage', data=msg, timeout=10) except exception: logger.exception('failed send moderation notice') user_prompt = '' payload = { 'task_id': task_id, 'task_text': task_text, 'images': images, 'user_prompt': user_prompt } # call hf retries attempt = 0 last_err = none attempt &lt;= retries: try: # prefer gradio_client fallback http resp = none use_gradio_client: try: resp = call_hf_via_gradio_client(payload['task_text'], payload.get('images', []), payload.get('user_prompt', '')) except hfquotaerror qe: # service quota exhausted schedule retry backoff logger.warning('hf quota error task task_id, qe) # determine current retry count (stored payload) retry_count = int(item.get('retry_count', 0)) retry_count += 1 # hf provides explicit retry_after, use otherwise exponential backoff qe.retry_after_seconds qe.retry_after_seconds &gt; 0: delay = qe.retry_after_seconds else: delay = min(3600, 60 * (2 ** (retry_count - 1))) retry_count &gt; hf_max_retries: logger.warning('max retries exceeded task giving task_id) save_result_to_redis(task_id, {'status': 'quota_exhausted', 'error': str(qe)}) tg_api_base chat_id: try: requests.post(tg_api_base + 'sendmessage', data={'chat_id': str(chat_id), 'text': ' ( ). .'}, timeout=10) except exception: logger.exception('failed notify user final quota') return # prepare task requeue new_task = dict(item) new_task['retry_count'] = retry_count # schedule delayed set schedule_delayed_task(new_task, delay) save_result_to_redis(task_id, {'status': 'scheduled_retry', 'retry_count': retry_count, 'next_try_in': delay}) tg_api_base chat_id: try: requests.post(tg_api_base + 'sendmessage', data={'chat_id': str(chat_id), 'text': f' {delay} .'}, timeout=10) except exception: logger.exception('failed notify user scheduled retry') return except exception: # non-quota error gradio_client. default fallback http # avoid sending posts non-api pages (causing 405). fallback # explicitly allowed via hf_allow_http_fallback hf_api_url looks # like api endpoint. logger.exception('gradio_client call failed') save_result_to_redis(task_id, {'status': 'error', 'error': 'gradio_client_failed'}) hf_allow_http_fallback: tg_api_base chat_id: try: requests.post(tg_api_base + 'sendmessage', data={'chat_id': str(chat_id), 'text': ' (gradio client). .'}, timeout=10) except exception: logger.exception('failed notify user gradio_client failure') return # hf_allow_http_fallback true ensure hf_api_url looks like api hf_api_url '/api/' hf_api_url: logger.warning('hf_api_url configured api endpoint; skipping http fallback') save_result_to_redis(task_id, {'status': 'error', 'error': 'no_http_fallback_available'}) tg_api_base chat_id: try: requests.post(tg_api_base + 'sendmessage', data={'chat_id': str(chat_id), 'text': ' hf ( http fallback).'}, timeout=10) except exception: logger.exception('failed notify user missing http fallback') return logger.info('gradio_client failed; hf_allow_http_fallback enabled hf_api_url looks like api falling back http') resp = none resp none: # attempt http fallback; surface error instead logger.error('no response gradio_client task http fallback disabled', task_id) save_result_to_redis(task_id, {'status': 'error', 'error': 'no_gradio_response'}) tg_api_base chat_id: try: requests.post(tg_api_base + 'sendmessage', data={'chat_id': str(chat_id), 'text': ' ( api). .'}, timeout=10) except exception: logger.exception('failed notify user no_gradio_response') return # response handling: look pdf_url pdf_base64 resp: raise runtimeerror('empty response hf') # gradio_client used returns dict 'file_bytes' possibly pdf_bytes = none file_name = none isinstance(resp, dict) 'file_bytes' resp resp.get('file_bytes'): pdf_bytes = resp.get('file_bytes') file_name = resp.get('file_name') # existing http-style responses pdf_bytes: pdf_url = none isinstance(resp, dict) 'pdf_url' resp: pdf_url = resp['pdf_url'] pdf_bytes = download_url(pdf_url) elif isinstance(resp, dict) 'pdf_base64' resp: pdf_bytes = base64.b64decode(resp['pdf_base64']) else: # try detect data response spaces return data files) isinstance(resp, dict) 'data' resp isinstance(resp['data'], list): # attempt find base64 blob file url el resp['data']: isinstance(el, dict) el.get('type') == 'pdf' el.get('data'): try: pdf_bytes = base64.b64decode(el.get('data')) break except exception: pass isinstance(el, dict) el.get('url'): try: pdf_bytes = download_url(el.get('url')) break except exception: pass # fallback: recognizable pdf pdf_bytes: save_result_to_redis(task_id, {'status': 'error', 'error': 'no_pdf_in_hf_response', 'response': resp}) tg_api_base chat_id: try: requests.post(tg_api_base + 'sendmessage', data={'chat_id': str(chat_id), 'text': 'hf .'}, timeout=10) except exception: logger.exception('failed report hf format error user') return # send pdf telegram bytes # hf returned file markdown file -&gt; route render_service pdf conversion file_name file_name.lower().endswith('.md') pdf_bytes: try: # create temporary render task id render_task_id = f"hf_render:{task_id}" # create task object task_text markdown content md_text = pdf_bytes.decode('utf-8', errors='replace') task_obj = { 'task_text': md_text, 'images': [], 'prompt': '', 'format': 'md', 'real_id': task_id } r.set(f"task:{render_task_id}", json.dumps(task_obj), ex=redis_ttl) # assign assignee render_service notify correct chat r.set(f"task_assignee:{render_task_id}", str(chat_id), ex=redis_ttl) # enqueue render r.lpush('render_queue', render_task_id) save_result_to_redis(task_id, {'status': 'render_queued', 'message': 'markdown received; render queued', 'render_task_id': render_task_id}) tg_api_base chat_id: try: requests.post(tg_api_base + 'sendmessage', data={'chat_id': str(chat_id), 'text': ' md pdf.'}, timeout=10) except exception: logger.exception('failed notify user queued render') return except exception: logger.exception('failed queue render markdown result') pdf_bytes: ok = false tg_api_base chat_id: ok = send_telegram_document(chat_id, pdf_bytes, filename=f'solution_{task_id}.pdf') # save redis (store base64 avoid external storage) save_result_to_redis(task_id, {'status': 'ok', 'pdf_base64': base64.b64encode(pdf_bytes).decode('ascii')}) return else: # unreachable normally save_result_to_redis(task_id, {'status': 'error', 'error': 'no_pdf_bytes'}) return except exception e: logger.exception('attempt error processing task attempt, task_id) last_err = str(e) attempt += 1 time.sleep(1 + attempt * 2) # retries save_result_to_redis(task_id, {'status': 'error', 'error': last_err}) tg_api_base chat_id: try: requests.post(tg_api_base + 'sendmessage', data={'chat_id': str(chat_id), 'text': ' , .'}, timeout=10) except exception: logger.exception('failed notify user final error') def main(): logger.info('starting hf_worker, concurrency=%s', worker_concurrency) # diagnostic: log sha256 file help confirm deployed version try: p = os.path.abspath(__file__) open(p, 'rb') fh: data = fh.read() h = hashlib.sha256(data).hexdigest() logger.info('hf_worker.py sha256=%s', h[:12]) except exception: logger.exception('failed compute hf_worker.py sha256') # start health server (non-blocking) render sees service web service def start_health(): web none: logger.warning('aiohttp available; health endpoint disabled') return port = int(os.environ.get('port', '8080')) async def health(request): # include quick metrics: hf_queue length uptime try: qlen = 0 try: qlen = r.llen('hf_queue') except exception: logger.exception('failed read hf_queue length') uptime = int(time.time() - start_time) return web.json_response({'status': 'ok', 'queue_length': qlen, 'uptime_seconds': uptime}) except exception: return web.json_response({'status': 'ok'}) # create new event loop run aiohttp apprunner avoid # setting signal handlers non-main thread def runner(): try: loop = asyncio.new_event_loop() asyncio.set_event_loop(loop) app = web.application() app.add_routes([web.get('/', health), web.get('/health', health)]) runner_obj = web.apprunner(app) loop.run_until_complete(runner_obj.setup()) site = web.tcpsite(runner_obj, '0.0.0.0', port) loop.run_until_complete(site.start()) logger.info('health server started port (background loop)', port) loop.run_forever() except exception: logger.exception('health server stopped') = threading.thread(target=runner, daemon=true) t.start() start_health() # start delayed-task mover thread try: mover = threading.thread(target=move_due_delayed_tasks, daemon=true) mover.start() logger.info('started delayed-task mover thread') except exception: logger.exception('failed start delayed-task mover') executor = threadpoolexecutor(max_workers=worker_concurrency) try: true: try: item = r.brpop('hf_queue', timeout=5) item: continue _, raw = item isinstance(raw, bytes): raw = raw.decode('utf-8') try: data = json.loads(raw) except exception: logger.exception('invalid json queue item') continue # submit thread pool executor.submit(process_task, data) except exception: logger.exception('worker loop error') time.sleep(1) except keyboardinterrupt: logger.info('shutting hf_worker') finally: executor.shutdown(wait=true) __name__ == '__main__': main()</file><file name="monitor.html">&lt;!doctype html&gt; &lt;html lang="ru"&gt; &lt;head&gt; &lt;meta charset="utf-8"&gt; &lt;meta name="viewport" content="width=device-width, initial-scale=1.0"&gt; &lt;title&gt;telegram bot monitor&lt;/title&gt; &lt;style&gt; body { font-family: arial, sans-serif; background: #f7f7f7; margin: 0; padding: 0; } .container { max-width: 700px; margin: 40px auto; background: #fff; border-radius: 8px; box-shadow: 0 2px 8px #0001; padding: 24px; } h1 { text-align: center; color: #2d6cdf; } .log-list { list-style: none; padding: 0; } .log-item { margin-bottom: 18px; border-bottom: 1px solid #eee; padding-bottom: 10px; } .user { color: #2d6cdf; font-weight: bold; } .cmd { color: #333; } .response { color: #008000; margin-top: 4px; } .timestamp { color: #888; font-size: 0.9em; } &lt;/style&gt; &lt;/head&gt; &lt;body&gt; &lt;div class="container"&gt; &lt;h1&gt;telegram bot monitor&lt;/h1&gt; &lt;div style="margin-bottom:18px;padding:12px;border:1px solid #eee;border-radius:6px;background:#fbfbfc"&gt; &lt;strong&gt; &lt;/strong&gt; &lt;div style="margin-top:8px"&gt; &lt;label&gt;bot username ( @): &lt;input id="uibot" style="width:220px;margin-left:8px" placeholder="my_bot"&gt;&lt;/label&gt; &lt;/div&gt; &lt;div style="margin-top:8px"&gt; &lt;label&gt;task id (, . id): &lt;input id="uitaskid" style="width:220px;margin-left:8px" placeholder=" id"&gt;&lt;/label&gt; &lt;/div&gt; &lt;div style="margin-top:8px"&gt; &lt;label&gt; ( ): &lt;input id="uirealid" style="width:220px;margin-left:8px" placeholder=" 42"&gt;&lt;/label&gt; &lt;/div&gt; &lt;div style="margin-top:8px"&gt; &lt;label&gt; ():&lt;/label&gt; &lt;input id="uiprompt" style="width:100%;max-width:640px;padding:8px" placeholder=" "&gt; &lt;/div&gt; &lt;div style="margin-top:8px"&gt; &lt;label&gt; : &lt;select id="uiformat" style="margin-left:8px"&gt;&lt;option value="md"&gt;markdown ( + )&lt;/option&gt;&lt;option value="md-table"&gt;markdown ()&lt;/option&gt;&lt;/select&gt;&lt;/label&gt; &lt;/div&gt; &lt;div style="margin-top:8px"&gt; &lt;label&gt; :&lt;/label&gt; &lt;textarea id="uitext" rows="3" style="width:100%;max-width:640px;padding:8px" placeholder=" "&gt;&lt;/textarea&gt; &lt;/div&gt; &lt;div style="margin-top:8px"&gt; &lt;label&gt; (): &lt;input id="uifiles" type="file" multiple accept="image/*"&gt;&lt;/label&gt; &lt;/div&gt; &lt;div style="margin-top:10px"&gt; &lt;button id="createopen" style="padding:8px 12px;background:#2d6cdf;color:#fff;border:none;border-radius:6px;cursor:pointer"&gt; telegram&lt;/button&gt; &lt;span id="createstatus" style="margin-left:12px;color:#2d6cdf"&gt;&lt;/span&gt; &lt;/div&gt; &lt;/div&gt; &lt;button id="clearbtn" style="margin-bottom:18px; background:#e74c3c; color:#fff; border:none; padding:8px 18px; border-radius:5px; cursor:pointer;"&gt; &lt;/button&gt; &lt;ul class="log-list" id="loglist"&gt; &lt;!-- --&gt; &lt;/ul&gt; &lt;/div&gt; &lt;script&gt; function genid() { return + date.now().tostring(36) + math.random().tostring(36).slice(2,8); } document.getelementbyid('createopen').addeventlistener('click', async function(){ const bot = document.getelementbyid('uibot').value.trim(); (!bot) { alert(' ( @)'); return; } let taskid = document.getelementbyid('uitaskid').value.trim(); (!taskid) { alert('task id . , .'); return; } const text = document.getelementbyid('uitext').value || ''; const realid = document.getelementbyid('uirealid').value.trim() || ''; const prompt = document.getelementbyid('uiprompt').value || ''; const outformat = document.getelementbyid('uiformat').value || 'md'; const files = document.getelementbyid('uifiles').files; const status = document.getelementbyid('createstatus'); status.textcontent = ' ...'; const fd = new formdata(); fd.append('task_id', taskid); fd.append('real_id', realid); fd.append('task_text', text); fd.append('prompt', prompt); fd.append('format', outformat); (let i=0;i&lt;files.length;i++) fd.append('images', files[i]); try { const res = await fetch('/create_task', { method: 'post', body: fd }); const j = await res.json(); (res.ok &amp;&amp; j.task_id) { status.textcontent = ' telegram...'; const url = 'https://t.me/' + encodeuricomponent(bot) + '?start=' + encodeuricomponent(j.task_id); window.open(url, '_blank'); settimeout(()=&gt;{ status.textcontent = ''; }, 3000); } else { status.textcontent = ': ' + (j.error||json.stringify(j)); } } catch (e) { status.textcontent = ' '; } }); async function fetchlogs() { try { const res = await fetch('/logs'); const logs = await res.json(); const loglist = document.getelementbyid('loglist'); loglist.innerhtml = ''; logs.foreach(log =&gt; { loglist.innerhtml += ` &lt;li class="log-item"&gt; &lt;span class="timestamp"&gt;${log.timestamp || ''}&lt;/span&gt;&lt;br&gt; &lt;span class="user"&gt;${log.username || 'user'}&lt;/span&gt;: &lt;span class="cmd"&gt;${log.command || ''}&lt;/span&gt; &lt;div class="response"&gt;: ${log.response || ''}&lt;/div&gt; &lt;/li&gt; `; }); } catch (e) { // } } document.getelementbyid('clearbtn').onclick = async function() { (confirm(' ?')) { await fetch('/clear_logs', {method: 'post'}); fetchlogs(); } }; setinterval(fetchlogs, 3000); fetchlogs(); &lt;/script&gt; &lt;/body&gt; &lt;/html&gt;</file><file name="monitor_backend.py">flask import flask, jsonify, send_from_directory, request, render_template_string import requests import redis import os import json app = flask(__name__) # config: ttl created keys (seconds) redis_ttl = int(os.environ.get('redis_ttl', '600')) # resolve bot username telegram_token redirects telegram_token = os.environ.get('telegram_token') bot_username = none telegram_token: try: resp = requests.get(f"https://api.telegram.org/bot{telegram_token}/getme", timeout=5) resp.raise_for_status() bot_username = resp.json().get('result', {}).get('username') except exception: bot_username = none # redis redis_url = os.environ.get("upstash_redis_url") r = redis.redis.from_url(redis_url, decode_responses=true) # () index_html = """ &lt;!doctype html&gt; &lt;html&gt; &lt;head&gt; &lt;meta charset="utf-8" /&gt; &lt;title&gt;monitor task simulator&lt;/title&gt; &lt;style&gt; body{font-family: arial, helvetica, sans-serif; margin:24px} .task{border:1px solid #ddd;padding:12px;margin:8px 0;border-radius:6px} .task a{color:#0b69ff;text-decoration:none} .small{font-size:0.9em;color:#666} .controls{margin-top:12px} &lt;/style&gt; &lt;/head&gt; &lt;body&gt; &lt;h1&gt;monitor task simulator&lt;/h1&gt; &lt;p class="small"&gt;click simulated task emulate hugo behaviour: create `task:&amp;lt;id&amp;gt;` redis, enqueue render job, redirect telegram bot &lt;code&gt;?start=&amp;lt;id&amp;gt;&lt;/code&gt;.&lt;/p&gt; {% tid, meta tasks.items() %} &lt;div class="task"&gt; &lt;strong&gt;task id: {{ tid }}&lt;/strong&gt; &lt;div&gt; {% meta.images meta.images|length &gt; 0 %} &lt;img src="{{ meta.images[0] }}" alt="thumb" style="height:90px;float:right;margin-left:12px;border:1px solid #eee;padding:4px;background:#fff;border-radius:4px"/&gt; {% endif %} &lt;div&gt;{{ meta.text }}&lt;/div&gt; &lt;/div&gt; &lt;div style="clear:both" class="controls"&gt; href="#" data-task="{{ tid }}" data-text="{{ meta.text|e }}" class="open"&gt;open bot (simulate)&lt;/a&gt; &lt;/div&gt; &lt;/div&gt; {% endfor %} &lt;hr/&gt; &lt;div style="margin:10px 0"&gt; &lt;button id="clear-all" style="background:#ef4444;color:#fff;border:none;padding:8px 12px;border-radius:6px;cursor:pointer"&gt;clear redis (tasks, queue, logs)&lt;/button&gt; &lt;button id="clear-logs" style="margin-left:8px;padding:8px 12px;border-radius:6px;cursor:pointer"&gt;clear bot logs&lt;/button&gt; &lt;/div&gt; &lt;h3&gt;custom task&lt;/h3&gt; &lt;form id="custom"&gt; &lt;label&gt;task id (eg. task-60-27-4): &lt;input name="task_id" value="task-60-99-1"/&gt;&lt;/label&gt; &lt;br/&gt; &lt;label&gt;text: &lt;input name="text" value=" " style="width:60%"/&gt;&lt;/label&gt; &lt;br/&gt; &lt;button&gt;simulate click&lt;/button&gt; &lt;/form&gt; &lt;script&gt; async function simulate(task_id, text){ const url = '/simulate_click'; const resp = await fetch(url, { method: 'post', headers: {'content-type':'application/json'}, body: json.stringify({task_id, text}) }); if(!resp.ok){ alert('failed create task: '+resp.statustext); return; } const j = await resp.json(); if(j.redirect){ window.location = j.redirect; } else if(j.tg_start_url){ window.location = j.tg_start_url; } else { alert('ok task created.'); } } document.queryselectorall('.open').foreach(el=&gt;{ el.addeventlistener('click', async (ev)=&gt;{ ev.preventdefault(); const tid = el.dataset.task; const text = el.dataset.text || (' '+tid); await simulate(tid, text); }); }); document.getelementbyid('clear-logs').addeventlistener('click', async ()=&gt;{ if(!confirm('clear bot logs?')) return; const r = await fetch('/clear_logs', {method:'post'}); if(r.ok) alert('bot logs cleared'); else alert('failed'); }); document.getelementbyid('clear-all').addeventlistener('click', async ()=&gt;{ if(!confirm('this delete temporary task keys, render queue bot logs redis. continue?')) return; const r = await fetch('/clear_all', {method:'post'}); if(!r.ok){ alert('failed clear: '+r.statustext); return; } const j = await r.json(); alert('cleared '+(j.deleted||0)+' keys'); }); document.getelementbyid('custom').addeventlistener('submit', async (ev)=&gt;{ ev.preventdefault(); const form = ev.currenttarget; const fd = new formdata(form); const id = fd.get('task_id'); const text = fd.get('text'); await simulate(id, text); }); &lt;/script&gt; &lt;/body&gt; &lt;/html&gt; """ @app.route('/') def index(): # try populate monitor real tasks index.json present) tasks = {} try: = os.path.dirname(__file__) idx_path = os.path.join(here, 'index.json') os.path.exists(idx_path): open(idx_path, 'r', encoding='utf-8') fh: data = json.load(fh) # collect n tasks least one named task interesting text n = 8 added = 0 import base64 def make_svg_data_url(title, page): # simple svg thumbnail page title (kept small) = f"""&lt;svg xmlns='http://www.w3.org/2000/svg' width='360' height='180'&gt; &lt;rect width='100%' height='100%' fill='#f7f9fc' stroke='#ddd'/&gt; &lt;text x='12' y='28' font-size='18' fill='#1f2937'&gt; {page}&lt;/text&gt; &lt;text x='12' y='56' font-size='14' fill='#374151'&gt;""" + (title.replace('&amp;','&amp;amp;').replace('&lt;','&amp;lt;')[:140]) + """&lt;/text&gt; &lt;/svg&gt;""" b = s.encode('utf-8') return 'data:image/svg+xml;base64,' + base64.b64encode(b).decode('ascii') entry data: added &gt;= n: break page = entry.get('page') tasks_list = entry.get('tasks') [] # explicit tasks, add separate simulated task tasks_list: idx, tlabel enumerate(tasks_list[:2]): added &gt;= n: break tid = f"hannova-p{page}-{idx+1}" text = f"{tlabel} {entry.get('title','').strip()}" thumb = make_svg_data_url(entry.get('title',''), page) tasks[tid] = {'text': text, 'images': [thumb]} added += 1 else: # explicit tasks, still add page sample added &lt; n: tid = f"hannova-p{page}-pg" text = f"{entry.get('title','')} {entry.get('workbook','')}" thumb = make_svg_data_url(entry.get('title',''), page) tasks[tid] = {'text': text, 'images': [thumb]} added += 1 except exception: tasks = { 'task-60-27-4': {'text': ' 60.27.4', 'images': []}, 'task-60-27-5': {'text': ' 60.27.5', 'images': []} } # fallback minimal tasks index.json missing empty tasks: tasks = { 'task-60-27-4': {'text': ' 60.27.4', 'images': []}, 'task-60-27-5': {'text': ' 60.27.5', 'images': []} } return render_template_string(index_html, tasks=tasks) # 100 @app.route('/logs') def get_logs(): logs = r.lrange('bot_logs', -100, -1) logs = [json.loads(log) log logs] return jsonify(logs) # redis: post {"task_id":"123","task_text":"...","images":[]} @app.route('/create_task', methods=['post']) def create_task(): # json multipart/form-data ( ) task_id = none task_text = '' images = [] request.content_type request.content_type.startswith('multipart/form-data'): # form fields task_id = request.form.get('task_id') none task_text = request.form.get('task_text', '') prompt = request.form.get('prompt', '') out_format = request.form.get('format', 'md') real_id = request.form.get('real_id', '') # files = request.files.getlist('images') f files: try: data = f.read() import base64 b64 = base64.b64encode(data).decode('ascii') mime = f.content_type 'application/octet-stream' data_url = f"data:{mime};base64,{b64}" images.append(data_url) except exception: continue else: try: data = request.get_json(force=true) except exception: return jsonify({'error': 'invalid json'}), 400 task_id = data.get('task_id') task_text = data.get('task_text', '') images = data.get('images', []) prompt = data.get('prompt', '') out_format = data.get('format', 'md') real_id = data.get('real_id', '') task_id: return jsonify({'error': 'task_id required'}), 400 obj = { 'task_text': task_text, 'images': images, 'prompt': prompt, 'format': out_format, 'real_id': real_id } r.set(f"task:{task_id}", json.dumps(obj), ex=redis_ttl) return jsonify({'status': 'ok', 'task_id': task_id}) @app.route('/simulate_click', methods=['post']) def simulate_click(): data = request.get_json() {} task_id = data.get('task_id') text = data.get('text') f" {task_id}" task_id: return jsonify({'error': 'task_id required'}), 400 task_obj = { 'task_text': text, 'images': [], 'prompt': '', 'format': 'png', 'real_id': task_id } try: r.set(f"task:{task_id}", json.dumps(task_obj), ex=redis_ttl) r.lpush('render_queue', task_id) r.set(f"task_pending:{task_id}", '1', ex=redis_ttl) except exception e: return jsonify({'error': str(e)}), 500 bot_username: tg_url = f"https://t.me/{bot_username}?start={task_id}" return jsonify({'redirect': tg_url}) return jsonify({'tg_start_url': f"https://t.me/?start={task_id}"}) # ( ) @app.route('/task/&lt;task_id&gt;') def get_task(task_id): raw = r.get(f"task:{task_id}") r.get(task_id) raw: return jsonify({'error': found'}), 404 try: return jsonify(json.loads(raw)) except exception: return jsonify({'raw': raw}) @app.route('/task_image/&lt;task_id&gt;/&lt;int:idx&gt;') def task_image(task_id, idx): """return image binary task images stored data urls external urls.""" raw = r.get(f"task:{task_id}") r.get(task_id) raw: return jsonify({'error': found'}), 404 try: task_obj = json.loads(raw) except exception: return jsonify({'error': 'invalid task data'}), 400 images = task_obj.get('images', []) idx &lt; 0 idx &gt;= len(images): return jsonify({'error': 'index range'}), 404 img = images[idx] # data url, decode return isinstance(img, str) img.startswith('data:'): try: header, b64 = img.split(',', 1) # header like data:image/png;base64 mime = header.split(':', 1)[1].split(';', 1)[0] import base64 data = base64.b64decode(b64) flask import response return response(data, mimetype=mime) except exception: return jsonify({'error': 'invalid data url'}), 400 # http(s) url, redirect isinstance(img, str) (img.startswith('http://') img.startswith('https://')): flask import redirect return redirect(img) # fallback: return text/plain return jsonify({'raw': img}) # @app.route('/clear_logs', methods=['post']) def clear_logs(): r.delete('bot_logs') return jsonify({'status': 'ok'}) @app.route('/clear_all', methods=['post']) def clear_all(): """delete known temporary keys used monitor/bot/render pipeline. intentionally targets keys/patterns use arbitrary keys). """ patterns = [ 'task:*', 'task_png:*', 'task_pdf:*', 'task_png_url:*', 'task_pdf_url:*', 'task_pending:*', 'task_ready:*', 'task_assignee:*', ] deleted = [] # collect keys patterns try: p patterns: try: found = r.keys(p) except exception: found = [] found: try: r.delete(*found) except exception: k found: try: r.delete(k) except exception: pass deleted.extend(found) # remove render_queue list bot_logs single ('render_queue', 'bot_logs'): try: r.exists(single): r.delete(single) deleted.append(single) except exception: pass except exception e: return jsonify({'error': str(e)}), 500 return jsonify({'deleted': len(deleted), 'keys': deleted[:200]}) __name__ == '__main__': app.run(host='0.0.0.0', port=8080)</file><file name="my_bot.py"># ---------------------------- # # ---------------------------- datetime import datetime def log_event(username, command, response): log = { "username": username, "command": command, "response": response, "timestamp": datetime.now().strftime("%y-%m-%d %h:%m:%s") } r.rpush("bot_logs", json.dumps(log)) r.ltrim("bot_logs", -100, -1) import os import json import io import time import requests import markdown md jinja2 import template import urllib.parse import tempfile import base64 # global browser instance + lock reuse browser = none browser_lock = none telegram import update, inputfile, inlinekeyboardbutton, inlinekeyboardmarkup telegram.ext import applicationbuilder, commandhandler, messagehandler, callbackqueryhandler, filters, contexttypes import asyncio import redis flask import flask import threading import logging dotenv import load_dotenv load_dotenv() # ---------------------------- # # ---------------------------- telegram_token = os.environ["telegram_token"] upstash_redis_url = os.environ["upstash_redis_url"] hf_api_url = "https://hf.space/embed/mingg93/fgoslib-qwen3/api/predict/" hf_token = os.environ["hf_token"] monitor_base_url = os.environ.get("monitor_base_url", "") redis_ttl = int(os.environ.get("redis_ttl", "600")) # ---------------------------- # # ---------------------------- logging.basicconfig(level=logging.info) logger = logging.getlogger(__name__) # ---------------------------- # redis # ---------------------------- r = redis.redis.from_url(upstash_redis_url, decode_responses=true) # ---------------------------- # # ---------------------------- def save_session(user_id, task_text, images=none, user_prompt=none, output_format="md", username=none, task_ids=none): key = f"session:{user_id}" data = { "user_id": user_id, "username": username "", "task_text": task_text, "images": images [], "user_prompt": user_prompt "", "output_format": output_format, "task_ids": task_ids [] } r.set(key, json.dumps(data), ex=redis_ttl) logger.info(f"saved session user {user_id}: {data}") def load_session(user_id): key = f"session:{user_id}" raw = r.get(key) raw: return json.loads(raw) return none def update_prompt(user_id, prompt): session = load_session(user_id) session: session["user_prompt"] = prompt r.set(f"session:{user_id}", json.dumps(session), ex=redis_ttl) logger.info(f"updated prompt user {user_id}: {prompt}") def update_format(user_id, output_format): session = load_session(user_id) session: session["output_format"] = output_format r.set(f"session:{user_id}", json.dumps(session), ex=redis_ttl) logger.info(f"updated output format user {user_id}: {output_format}") # ---------------------------- # hf api # ---------------------------- def call_hf_api(task_text, images=none, user_prompt="", output_format="md"): payload = { "task_text": task_text, "user_prompt": user_prompt, "images": images [], "output_format": output_format } headers = {"authorization": f"bearer {hf_token}"} logger.info(f"calling hf api payload: {payload}") resp = requests.post(hf_api_url, json=payload, headers=headers) resp.raise_for_status() return resp.json() # note: pdf/png rendering performed external render service. # bot longer performs headless rendering locally. # ---------------------------- # # ---------------------------- async def start(update: update, context: contexttypes.default_type): user_id = update.message.from_user.id username = update.message.from_user.username str(user_id) # (taskid) redis context.args: task_id = context.args[0] # : task:&lt;id&gt; &lt;id&gt; raw = r.get(f"task:{task_id}") r.get(task_id) raw: try: task_obj = json.loads(raw) task_text = task_obj.get("task_text") task_obj.get("text") task_obj.get("content") "" images = task_obj.get("images", []) prompt = task_obj.get("prompt", "") out_format = task_obj.get("format", "md") except exception: task_text = raw images = [] prompt = "" out_format = "md" # ( username) task_id session.task_ids (append ) existing = load_session(user_id) {} task_ids = existing.get('task_ids', []) isinstance(existing, dict) else [] task_id task_ids: task_ids.append(task_id) save_session(user_id, task_text, images, user_prompt=prompt, output_format=out_format, username=username, task_ids=task_ids) # task -&gt; user try: # ttl r.set(f"task_assignee:{task_id}", user_id, ex=redis_ttl) # , assigned_user_id ttl try: task_obj['assigned_user_id'] = user_id r.set(f"task:{task_id}", json.dumps(task_obj), ex=redis_ttl) except exception: pass except exception: logger.exception(' task-&gt;user redis') # markdown ( data urls) md_lines = [] md_lines.append(f"# {task_id}\n") prompt: md_lines.append(f"**:** {prompt}\n") md_lines.append("## :\n") md_lines.append(task_text + "\n") images: md_lines.append('\n## :\n') idx, img enumerate(images): # monitor_base_url /task_image/ monitor_base_url: base = monitor_base_url.rstrip('/') img_url = f"{base}/task_image/{task_id}/{idx}" md_lines.append(f"![]({img_url})\n") else: # data url, isinstance(img, str) img.startswith('data:'): md_lines.append(f"![]({img})\n") elif isinstance(img, str) img.startswith('http'): md_lines.append(f"![]({img})\n") else: # / md_lines.append(f"- {img}\n") md_content = "\n".join(md_lines) # convert markdown -&gt; html render pdf (mathjax enabled) using headless chromium title = f" {task_obj.get('real_id') task_id}" try: html_body = md.markdown(md_content, extensions=['extra', 'tables']) except exception: # fallback: wrap raw md pre html_body = f"&lt;pre&gt;{md_content}&lt;/pre&gt;" html_template = f"""&lt;!doctype html&gt; &lt;html&gt; &lt;head&gt; &lt;meta charset="utf-8"&gt; &lt;meta name="viewport" content="width=device-width,initial-scale=1"&gt; &lt;title&gt;{title}&lt;/title&gt; &lt;style&gt; body{{font-family: dejavu sans, arial, sans-serif; padding:20px;}} img{{max-width:100%;height:auto;}} pre{{white-space:pre-wrap;}} &lt;/style&gt; &lt;script src="https://cdn.jsdelivr.net/npm/mathjax@3/es5/tex-mml-chtml.js"&gt;&lt;/script&gt; &lt;/head&gt; &lt;body&gt; {html_body} &lt;/body&gt; &lt;/html&gt;""" # : url s3, base64 redis response = none kb = inlinekeyboardmarkup([[inlinekeyboardbutton("", callback_data=f"solve:{task_id}"), inlinekeyboardbutton("", callback_data=f"del:{task_id}")]]) try: png_url = r.get(f"task_png_url:{task_id}") r.get(f"task_pdf_url:{task_id}") png_b64 = none png_url: isinstance(png_url, bytes): png_url = png_url.decode('utf-8') # try: resp = requests.get(png_url, timeout=30) resp.raise_for_status() content = resp.content # decide type url png_url.lower().endswith('.png'): await update.message.reply_photo(photo=content, reply_markup=kb) else: await update.message.reply_document(document=inputfile(io.bytesio(content), filename=f"task_{task_obj.get('real_id') task_id}.pdf"), reply_markup=kb) response = f" {task_obj.get('real_id') task_id} ." except exception: logger.exception(' / url') else: # check render-worker result keys may store url base64 task_pdf_result task_result) alt = r.get(f"task_pdf_result:{task_id}") r.get(f"task_result:{task_id}") alt: isinstance(alt, bytes): alt = alt.decode('utf-8') # alt looks like url, treat otherwise treat base64 blob isinstance(alt, str) alt.startswith('http'): png_url = alt else: png_b64 = alt else: # check base64 blobs png_b64 = r.get(f"task_png:{task_id}") r.get(f"task_pdf:{task_id}") png_b64: isinstance(png_b64, bytes): png_b64 = png_b64.decode('ascii') try: data = base64.b64decode(png_b64) # know type; try png first try: await update.message.reply_photo(photo=data, reply_markup=kb) response = f" {task_obj.get('real_id') task_id} ." except exception: await update.message.reply_document(document=inputfile(io.bytesio(data), filename=f"task_{task_obj.get('real_id') task_id}.pdf"), reply_markup=kb) response = f" {task_obj.get('real_id') task_id} ." except exception: logger.exception(' base64') except exception: logger.exception(' ') # ( ) response: try: pending = r.get(f"task_pending:{task_id}") pending: r.lpush('render_queue', task_id) r.set(f"task_pending:{task_id}", '1', ex=redis_ttl) response = f" {task_obj.get('real_id') task_id} . , telegram ." else: response = f" {task_obj.get('real_id') task_id} , ." except exception: logger.exception(' ') # (response ) try: await update.message.reply_text(response) except exception: logger.exception(' response ') log_event(username, f"/start {task_id}", response) return else: response = f" {task_id} . taskid ." await update.message.reply_text(response) log_event(username, f"/start {task_id}", response) return await update.message.reply_text( "! ( + ), /prompt &lt;&gt; . .\n" " , /format md /format pdf." ) async def handle_task(update: update, context: contexttypes.default_type): user_id = update.message.from_user.id username = update.message.from_user.username str(user_id) task_text = update.message.text "" task_text update.message.photo: await update.message.reply_text(", .") log_event(username, "(empty)", ", .") return # images = [] update.message.photo: photo update.message.photo: images.append(photo.file_id) # task:&lt;local_id&gt; local_task_id = f"local-{user_id}-{int(time.time())}" task_obj = { 'task_text': task_text, 'images': images, 'prompt': '', 'format': 'md', 'real_id': '' } try: r.set(f"task:{local_task_id}", json.dumps(task_obj), ex=redis_ttl) except exception: logger.exception(' redis') # task_id session.task_ids (append ) existing = load_session(user_id) {} task_ids = existing.get('task_ids', []) isinstance(existing, dict) else [] local_task_id task_ids: task_ids.append(local_task_id) save_session(user_id, task_text, images, username=username, task_ids=task_ids) response = f" id {local_task_id}. /prompt ." await update.message.reply_text(response) log_event(username, task_text "[photo]", response) async def handle_prompt(update: update, context: contexttypes.default_type): user_id = update.message.from_user.id username = update.message.from_user.username str(user_id) prompt = " ".join(context.args) prompt: response = " /prompt &lt; &gt;" await update.message.reply_text(response) log_event(username, f"/prompt", response) return update_prompt(user_id, prompt) response = " ! ..." await update.message.reply_text(response) log_event(username, f"/prompt {prompt}", response) session = load_session(user_id) session: response = " ." await update.message.reply_text(response) log_event(username, f"/prompt {prompt}", response) return try: result = call_hf_api( task_text=session["task_text"], images=session["images"], user_prompt=session.get("user_prompt", ""), output_format=session.get("output_format", "md") ) # pdf session.get("output_format") == "pdf" "pdf" result: pdf_url = result["pdf"] # hf pdf_bytes = requests.get(pdf_url).content await update.message.reply_document(document=inputfile(io.bytesio(pdf_bytes), filename="solution.pdf")) log_event(username, f"/prompt {prompt}", "[pdf sent]") elif "text" result: await update.message.reply_text(result["text"]) log_event(username, f"/prompt {prompt}", result["text"]) else: response = " ." await update.message.reply_text(response) log_event(username, f"/prompt {prompt}", response) except exception e: logger.exception(" hf api") response = f" : {e}" await update.message.reply_text(response) log_event(username, f"/prompt {prompt}", response) async def callback_query_handler(update: update, context: contexttypes.default_type): query = update.callback_query await query.answer() data = query.data "" user = query.from_user username = user.username str(user.id) data.startswith('solve:'): task_id = data.split(':',1)[1] raw = r.get(f"task:{task_id}") r.get(task_id) raw: await query.edit_message_text(' .') return try: task_obj = json.loads(raw) except exception: await query.edit_message_text(' .') return # : message , # / caption, # fallback. msg_text = ' (hf). , .' try: query.message getattr(query.message, 'text', none): await query.edit_message_text(msg_text) else: # try editing caption (works media messages) try: await query.edit_message_caption(msg_text) except exception: # fallback: send separate message chat await context.bot.send_message(chat_id=query.message.chat_id, text=msg_text) except exception: logger.exception('failed edit/notify message solve click') try: await context.bot.send_message(chat_id=query.message.chat_id, text=msg_text) except exception: logger.exception('failed send fallback notification user') # hf payload = { 'task_id': task_id, 'chat_id': query.message.chat_id, 'task_text': task_obj.get('task_text',''), 'images': task_obj.get('images', []), 'user_prompt': task_obj.get('prompt','') '' } try: # hf, pending_key = f"task_pending_hf:{task_id}" pending = r.get(pending_key) pending: # push hf_queue (worker uses brpop) r.lpush('hf_queue', json.dumps(payload)) r.set(pending_key, '1', ex=redis_ttl) log_event(username, f"enqueue_hf {task_id}", 'enqueued') else: log_event(username, f"enqueue_hf {task_id}", 'already_pending') except exception e: logger.exception(' hf_queue') await context.bot.send_message(chat_id=query.message.chat_id, text=f' : {e}') log_event(username, f"solve {task_id}", f"error: {e}") return elif data.startswith('del:'): task_id = data.split(':',1)[1] # try: r.delete(f"task:{task_id}") r.delete(f"task_assignee:{task_id}") r.delete(f"task_result:{task_id}") # session, await query.edit_message_text(' .') await context.bot.send_message(chat_id=query.message.chat_id, text=f' {task_id} .') log_event(username, f"delete {task_id}", 'deleted') except exception e: logger.exception(' ') await query.edit_message_text(f' : {e}') log_event(username, f"delete {task_id}", f'error: {e}') else: await query.edit_message_text(' .') # ---------------------------- # # ---------------------------- # --- flask http server render health check --- app = flask(__name__) @app.route("/") def health(): return "bot running!" def start_flask(): port = int(os.environ.get("port", 10000)) app.run(host="0.0.0.0", port=port) def run_bot(): bot_app = applicationbuilder().token(telegram_token).build() bot_app.add_handler(commandhandler("start", start)) bot_app.add_handler(commandhandler("prompt", handle_prompt)) bot_app.add_handler(callbackqueryhandler(callback_query_handler)) bot_app.add_handler(messagehandler(filters.text | filters.photo, handle_task)) logger.info(" ...") bot_app.run_polling() __name__ == "__main__": flask_thread = threading.thread(target=start_flask) flask_thread.start() run_bot()</file><file name="render_service.py">#!/usr/bin/env python3 """ render worker service. listens redis `render_queue` (brpop) task ids. task - loads `task:&lt;id&gt;` redis - builds html markdown) injects katex math rendering - renders png content fits single page, otherwise renders pdf - uploads result s3 configured) stores base64 redis `task_png:&lt;id&gt;`/`task_pdf:&lt;id&gt;` - notifies telegram user `task_assignee:&lt;id&gt;` exists sending file via bot api environment variables: - upstash_redis_url (required) - telegram_token (optional, sending files) - s3_bucket, aws_access_key_id, aws_secret_access_key, s3_endpoint (optional) - redis_ttl (seconds stored keys, default 600) run docker image playwright browsers installed (dockerfile.render provided). """ import os import time import base64 import json import logging import asyncio typing import optional import redis import requests import boto3 botocore.exceptions import botocoreerror, clienterror import markdown md aiohttp import web playwright.async_api import async_playwright logging.basicconfig(level=logging.info) logger = logging.getlogger('render_service') upstash_redis_url = os.environ.get('upstash_redis_url') upstash_redis_url: raise runtimeerror('upstash_redis_url required') r = redis.redis.from_url(upstash_redis_url, decode_responses=false) redis_ttl = int(os.environ.get('redis_ttl', '600')) telegram_token = os.environ.get('telegram_token') # optional s3 config s3_bucket = os.environ.get('s3_bucket') s3_endpoint = os.environ.get('s3_endpoint') # e.g. https://s3.amazonaws.com spaces endpoint aws_key = os.environ.get('aws_access_key_id') aws_secret = os.environ.get('aws_secret_access_key') def s3_client(): s3_bucket: return none kwargs = {} s3_endpoint: kwargs['endpoint_url'] = s3_endpoint return boto3.client('s3', aws_access_key_id=aws_key, aws_secret_access_key=aws_secret, **kwargs) def upload_to_s3(bytes_data: bytes, key: str, content_type: str) -&gt; optional[str]: client = s3_client() client none: return none try: client.put_object(bucket=s3_bucket, key=key, body=bytes_data, contenttype=content_type) # build url s3_endpoint: return f"{s3_endpoint.rstrip('/')}/{s3_bucket}/{key}" else: return f"https://{s3_bucket}.s3.amazonaws.com/{key}" except (botocoreerror, clienterror) e: logger.exception('s3 upload failed') return none def build_html(task_obj: dict) -&gt; str: """return html document string katex included client-side math rendering.""" text = task_obj.get('task_text', '') '' try: body = md.markdown(text, extensions=['extra', 'tables']) except exception: body = f"&lt;pre&gt;{text}&lt;/pre&gt;" # build html without using f-string avoid brace-escaping issues port_str = os.environ.get('port', os.environ.get('http_port', '8080')) base_url = 'http://127.0.0.1:' + port_str head = ("""&lt;!doctype html&gt; &lt;html&gt; &lt;head&gt; &lt;meta charset="utf-8" /&gt; &lt;meta name="viewport" content="width=device-width,initial-scale=1" /&gt; &lt;title&gt;task&lt;/title&gt; """) # reference local katex resources served /static/ head += "\n &lt;link rel=\"stylesheet\" href=\"" + base_url + "/static/katex/katex.min.css\"&gt;" head += "\n &lt;script defer src=\"" + base_url + "/static/katex/katex.min.js\"&gt;&lt;/script&gt;" head += "\n &lt;script defer src=\"" + base_url + "/static/katex/contrib/auto-render.min.js\"&gt;&lt;/script&gt;" head += "\n &lt;style&gt;body{font-family: dejavu sans, arial, sans-serif; padding:20px;} img{max-width:100%; height:auto;} pre{white-space:pre-wrap;}&lt;/style&gt;\n&lt;/head&gt;\n&lt;body&gt;" # js snippet: use normal js braces doubling) since f-string js = (""" &lt;script&gt; document.addeventlistener('domcontentloaded', ()=&gt;{ (window.rendermathinelement) { try { rendermathinelement(document.body, { delimiters: [ {left: '$$', right: '$$', display: true}, {left: '$', right: '$', display: false}, {left: '\\(', right: '\\)', display: false}, {left: '\\[', right: '\\]', display: true} ] }); } catch(e) { console.error(e); } } }); &lt;/script&gt; """) html = head + body + js + "\n&lt;/body&gt;\n&lt;/html&gt;" return html async def render_task(task_id: str): raw = r.get(f"task:{task_id}") raw: logger.warning('task found redis', task_id) return try: task_obj = json.loads(raw.decode('utf-8')) except exception: logger.exception('failed parse task json') return html = build_html(task_obj) async async_playwright() p: browser = await p.chromium.launch(args=['--no-sandbox', '--disable-setuid-sandbox']) # create context higher device scale sharper screenshots context = await browser.new_context(viewport={'width': 1024, 'height': 1200}, device_scale_factor=2) page = await context.new_page() try: await page.set_content(html, wait_until='networkidle') # wait katex auto-render finish present) try: await page.wait_for_selector('.katex', timeout=3000) except exception: pass # give katex moment render fallback try: await page.evaluate('''() =&gt; { (window.rendermathinelement) { return true; } }''') except exception: pass # previously used page height decide png vs pdf. switch pdf-only # default pdf generation faster matches requirements. # keep png generation code future use (left reference). logger.info('rendering task pdf (forced)', task_id) pdf_bytes = await page.pdf(format='a4', print_background=true) s3_bucket: key = f"renders/{task_id}.pdf" url = upload_to_s3(pdf_bytes, key, 'application/pdf') url: r.set(f"task_pdf_url:{task_id}", url, ex=redis_ttl) else: r.set(f"task_pdf:{task_id}", base64.b64encode(pdf_bytes).decode('ascii'), ex=redis_ttl) else: r.set(f"task_pdf:{task_id}", base64.b64encode(pdf_bytes).decode('ascii'), ex=redis_ttl) await notify_user_with_file(task_id, pdf_bytes, is_pdf=true) # --- png generation code (kept future use) --- # scroll_height = await page.evaluate('() =&gt; document.body.scrollheight') # logger.info('task scrollheight=%s', task_id, scroll_height) # scroll_height &lt;= 1400: # png_bytes = await page.screenshot(full_page=true, type='png') # s3_bucket: # key = f"renders/{task_id}.png" # url = upload_to_s3(png_bytes, key, 'image/png') # url: # r.set(f"task_png_url:{task_id}", url, ex=redis_ttl) # else: # r.set(f"task_png:{task_id}", base64.b64encode(png_bytes).decode('ascii'), ex=redis_ttl) # else: # r.set(f"task_png:{task_id}", base64.b64encode(png_bytes).decode('ascii'), ex=redis_ttl) # await notify_user_with_file(task_id, png_bytes, is_pdf=false) except exception: logger.exception('render failed task_id) finally: try: await page.close() except exception: pass try: await context.close() except exception: pass try: await browser.close() except exception: pass async def notify_user_with_file(task_id: str, file_bytes: bytes, is_pdf: bool): # try send generated file assignee via telegram bot api try: assignee = r.get(f"task_assignee:{task_id}") assignee: chat_id = assignee.decode('utf-8') isinstance(assignee, bytes) else str(assignee) telegram_token: url = f"https://api.telegram.org/bot{telegram_token}/" is_pdf: api = url + 'senddocument' files = {'document': (f'task_{task_id}.pdf', file_bytes, 'application/pdf')} else: api = url + 'sendphoto' files = {'photo': (f'task_{task_id}.png', file_bytes, 'image/png')} data = {'chat_id': chat_id} # attach inline keyboard user immediately sees actions file arrives try: import json _json reply_markup = { 'inline_keyboard': [[ {'text': '', 'callback_data': f'solve:{task_id}'}, {'text': '', 'callback_data': f'del:{task_id}'} ]] } data['reply_markup'] = _json.dumps(reply_markup, ensure_ascii=false) except exception: logger.exception('failed build reply_markup') resp = requests.post(api, data=data, files=files, timeout=30) resp.status_code // 100 != 2: logger.warning('telegram send failed: resp.status_code, resp.text) else: logger.info('sent rendered file user task chat_id, task_id) else: logger.info('telegram_token set; skipping send user task assignee, task_id) else: logger.info('no assignee task skipping direct send', task_id) except exception: logger.exception('failed notify user task task_id) async def worker_loop(): logger.info('render worker started, waiting tasks...') true: try: # brpop returns tuple (queue, value) none timeout item = r.brpop('render_queue', timeout=5) item: await asyncio.sleep(0.1) continue _, task_id = item isinstance(task_id, bytes): task_id = task_id.decode('utf-8') logger.info('got task queue', task_id) # mark pending -&gt; handled r.set(f"task_pending:{task_id}", '1', ex=redis_ttl) await render_task(task_id) # mark ready r.set(f"task_ready:{task_id}", '1', ex=redis_ttl) r.delete(f"task_pending:{task_id}") except exception: logger.exception('worker loop error') await asyncio.sleep(1) async def start_services(): # ensure playwright browsers installed running container logger.info('starting render service') # start background worker worker_task = asyncio.create_task(worker_loop()) # small http server render health checks basic status async def health(request): return web.response(text='ok') app = web.application() # serve local static files (e.g. katex resources) ./static static_path = os.path.abspath('./static') try: os.path.exists(static_path): os.makedirs(static_path, exist_ok=true) logger.info('created missing static directory static_path) except exception: logger.exception('failed ensure static directory exists: static_path) os.path.isdir(static_path): logger.warning('static path directory, skipping static route: static_path) else: app.router.add_static('/static/', path=static_path, show_index=false) app.add_routes([web.get('/', health), web.get('/health', health)]) runner = web.apprunner(app) await runner.setup() port = int(os.environ.get('port', os.environ.get('http_port', '8080'))) site = web.tcpsite(runner, '0.0.0.0', port) await site.start() logger.info('http server listening port port) try: # keep main alive worker runs true: await asyncio.sleep(3600) except asyncio.cancellederror: logger.info('shutdown requested') finally: worker_task.cancel() try: await worker_task except exception: pass await runner.cleanup() def main(): asyncio.run(start_services()) __name__ == '__main__': main()</file><file name="requirements.bot.txt">python-telegram-bot==20.6 redis==5.3.0 requests==2.31.0 python-dotenv==1.0.0 flask==3.0.3 # jinja2 comes dependency flask pin explicitly needed jinja2==3.1.2 markdown==3.5.2 # markdown used `my_bot.py` deployment; keep bot deploys succeed.</file><file name="requirements.hf.txt">requests&gt;=2.28 redis&gt;=4.5 python-dotenv&gt;=1.0 aiohttp&gt;=3.8 gradio_client&gt;=0.10.1</file><file name="requirements.render.txt">playwright==1.40.0 boto3==1.28.0 markdown==3.5.2 redis==5.3.0 python-dotenv==1.0.0 requests==2.31.0 aiohttp==3.9.4 # optional: add image libraries need server-side png generation (pillow) # pillow==10.0.0</file><file name="requirements.txt">--requirement requirements.bot.txt --requirement requirements.render.txt # note: dependencies split two files clarity: # - requirements.bot.txt : dependencies needed run telegram bot + monitor # - requirements.render.txt : dependencies render service (playwright, s3, etc.) # still install everything via file includes sets.</file><file name="simulation_telegram_bot.md"># simulation / test: telegram bot redirect : staging , telegram- `start=&lt;taskid&gt;`. : (post `/.netlify/functions/task`) `taskid`. `https://t.me/&lt;bot_username&gt;?start=&lt;taskid&gt;` . : - (`test-telegram.html`), - (express) render/heroku/ , - flow. prerequisites / - `hugo_bot_username` `@` ( hugo `hugo_...`). - `upstash_redis_rest_url` `upstash_redis_rest_token` `task`. - () `telegram_bot_token` / `telegram_chat_id` , telegram. 1) () - `test-telegram.html` : ```html &lt;!doctype html&gt; &lt;meta charset="utf-8"&gt; &lt;title&gt;test telegram open&lt;/title&gt; &lt;body&gt; &lt;h3&gt;test telegram&lt;/h3&gt; &lt;p&gt;replace &lt;code&gt;bot_username&lt;/code&gt; &lt;code&gt;task_id&lt;/code&gt; below.&lt;/p&gt; id="open" href="https://t.me/bot_username?start=task_id" target="_blank"&gt;open bot task_id&lt;/a&gt; &lt;/body&gt; &lt;/html&gt; ``` - , telegram ( -). 2) ( ) - , post `/.netlify/functions/task` json `taskid`. - `taskid` `window.open('https://t.me/' + bot_username + '?start=' + taskid)`. - , `taskid` . 3) redirect (express) ```js // server.js (express) const express = require('express'); const app = express(); const bot = process.env.bot_username; // @ app.get('/go-to-bot', (req, res) =&gt; { const taskid = req.query.taskid || ''; const turl = `https://t.me/${bot}?start=${encodeuricomponent(taskid)}`; res.redirect(302, turl); }); app.listen(process.env.port || 3000); ``` - endpoint ( render). `https://your-bot-host/go-to-bot?taskid=&lt;taskid&gt;` `t.me`. 4) - post `taskid` `taskid`. - , `clientid` payload; `clientid` . 5) - network xhr: , post `/.netlify/functions/task` json `{ ok: true, taskid: "..." }`. - taskid , `clientid` `set nx` (upstash) fallback get/set. : readme telegram- `taskid`. `static/test-telegram.html` . readme, .</file></source>